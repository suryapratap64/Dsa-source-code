function sequenceDNA() {
    let inputSequence = document.getElementById('inputDNA').value.trim();
    let outputResult = document.getElementById('outputResult');

    // Perform DNA sequencing logic here (you can replace this with your actual sequencing algorithm)
    let sequencedDNA = "Result will appear here."; // Replace this line with your sequencing result

    outputResult.innerHTML = sequencedDNA;
}
